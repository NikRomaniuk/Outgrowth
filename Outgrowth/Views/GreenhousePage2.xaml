<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Outgrowth.ViewModels"
             x:Class="Outgrowth.Views.GreenhousePage2"
             x:DataType="viewModels:GreenhouseViewModel">
    
    <Grid ColumnDefinitions="Auto,*,Auto">
        
        <!-- Layer 1: Background (spans all columns) -->
        <BoxView Grid.ColumnSpan="3" Color="#0A0E1A" />
        
        <!-- TODO: Replace with actual greenhouse background image when available -->
        <Image Grid.ColumnSpan="3"
               Source="dotnet_bot.png" 
               Aspect="AspectFill"
               Opacity="0.2"/>
        
        <!-- Left Gutter: Placeholder for maintaining column width -->
        <Grid Grid.Column="0"
              x:Name="LeftGutterPlaceholder"
              BackgroundColor="Transparent"
              WidthRequest="300"
              HeightRequest="500"
              Margin="20,0,0,0"
              HorizontalOptions="Start"
              VerticalOptions="Center"
              InputTransparent="True"
              ZIndex="100"/>
        
        <!-- Center Column: Environment (16:9 Aspect Ratio, Centered, Scaled by Height) -->
        <ContentView x:Name="EnvironmentWrapper"
                     Grid.Column="1"
                     HorizontalOptions="Center"
                     VerticalOptions="Fill"
                     ZIndex="100">
            
            <!-- Environment container with 16:9 aspect ratio - scales based on screen height -->
            <AbsoluteLayout x:Name="EnvironmentContainer"
                           HorizontalOptions="Fill"
                           VerticalOptions="Fill">
                
                <!-- Content Container: Contains all pots and future furniture items -->
                <!-- This container moves as a whole unit when scrolling -->
                <!-- Uses absolute coordinate system: X range from -9600 to 9600, Y range from -540 to 540 -->
                <!-- Container width = 19200 pixels (19200 * 1), height = 1080 pixels (1080 * 1) -->
                <!-- Center of container X = 9600 (19200 / 2), Y = 540 (1080 / 2) -->
                <!-- Scale: 1:1 pixel-to-logical-unit ratio (1 logical unit = 1 pixel) -->
                <!-- Pots are added dynamically from code-behind using PotObject instances -->
                <AbsoluteLayout x:Name="ContentContainer"
                               WidthRequest="19200"
                               HeightRequest="1080"
                               AbsoluteLayout.LayoutBounds="0,0,19200,1080"
                               AbsoluteLayout.LayoutFlags="None">
                    <!-- Pots will be added here programmatically -->
                </AbsoluteLayout>
                
            </AbsoluteLayout>
        </ContentView>
        
        <!-- Right Gutter: Hub Button -->
        <AbsoluteLayout Grid.Column="2"
                       x:Name="HubButtonLayout"
                       HorizontalOptions="End"
                       VerticalOptions="Fill"
                       ZIndex="1000">
            <Border x:Name="HubButton"
                    AbsoluteLayout.LayoutBounds="1,1,AutoSize,AutoSize"
                    AbsoluteLayout.LayoutFlags="XProportional,YProportional"
                    StrokeThickness="0"
                    BackgroundColor="Transparent"
                    HeightRequest="300"
                    WidthRequest="300"
                    Margin="0,0,20,20">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnHubClicked"/>
                </Border.GestureRecognizers>
                <Grid>
                    <BoxView Color="#1E3A5F"
                            CornerRadius="10"
                            Opacity="0.5"/>
                    <Label Text="🛰️"
                           FontSize="{DynamicResource ButtonIconSize}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                    <Label Text="[Hub Image]"
                           FontSize="{DynamicResource ButtonPlaceholderSize}"
                           HorizontalOptions="Center"
                           VerticalOptions="End"
                           Margin="0,0,0,10"
                           TextColor="White"
                           Opacity="0.7"/>
                </Grid>
            </Border>
        </AbsoluteLayout>
        
        <!-- Left Gutter: Liquids Panel (overlapping with Seeds Panel) -->
        <AbsoluteLayout Grid.Column="0"
                       x:Name="LiquidsPanelWrapper"
                       HorizontalOptions="Start"
                       VerticalOptions="Fill"
                       ZIndex="1000">
            <!-- Main Liquids Panel -->
            <Grid x:Name="LiquidsPanel"
                  AbsoluteLayout.LayoutBounds="0,0.5,AutoSize,AutoSize"
                  AbsoluteLayout.LayoutFlags="YProportional"
                  WidthRequest="250"
                  HeightRequest="500"
                  Margin="20,0,0,0"
                  IsVisible="False"
                  InputTransparent="True">
                <Border StrokeThickness="2"
                        Stroke="#4CAF50"
                        BackgroundColor="#1A1A1A"
                        Padding="15"
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>
                    <Grid RowDefinitions="Auto,*">
                        <Label Grid.Row="0"
                               Text="Liquids"
                               FontSize="{DynamicResource ResourcePanelTitleSize}"
                               FontAttributes="Bold"
                               TextColor="White"
                               VerticalOptions="Center"
                               Margin="0,0,0,10"/>
                        
                        <ScrollView Grid.Row="1"
                                    VerticalOptions="Fill"
                                    Orientation="Vertical">
                            <VerticalStackLayout x:Name="LiquidsList"
                                                 Spacing="10">
                                <!-- Liquid items will be added here dynamically -->
                            </VerticalStackLayout>
                        </ScrollView>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- SelectedLiquidPanel removed; will be created dynamically later -->
        </AbsoluteLayout>
        
        <!-- Left Gutter: Seeds Panel (overlapping with Liquids Panel) -->
        <AbsoluteLayout Grid.Column="0"
                       x:Name="SeedsPanelWrapper"
                       HorizontalOptions="Start"
                       VerticalOptions="Fill"
                       ZIndex="1000">
            <!-- Main Seeds Panel -->
            <Grid x:Name="SeedsPanel"
                  AbsoluteLayout.LayoutBounds="0,0.5,AutoSize,AutoSize"
                  AbsoluteLayout.LayoutFlags="YProportional"
                  WidthRequest="250"
                  HeightRequest="500"
                  Margin="20,0,0,0"
                  IsVisible="False"
                  InputTransparent="True">
                <Border StrokeThickness="2"
                        Stroke="#4CAF50"
                        BackgroundColor="#1A1A1A"
                        Padding="15"
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>
                    <Grid RowDefinitions="Auto,*">
                        <Label Grid.Row="0"
                               Text="Seeds"
                               FontSize="{DynamicResource ResourcePanelTitleSize}"
                               FontAttributes="Bold"
                               TextColor="White"
                               VerticalOptions="Center"
                               Margin="0,0,0,10"/>
                        
                        <ScrollView Grid.Row="1"
                                    VerticalOptions="Fill"
                                    Orientation="Vertical">
                            <VerticalStackLayout x:Name="SeedsList"
                                                 Spacing="10">
                                <!-- Seed items will be added here dynamically -->
                            </VerticalStackLayout>
                        </ScrollView>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- SelectedSeedPanel removed; will be created dynamically later -->
        </AbsoluteLayout>
        
        <!-- BottomPanelsContainer removed: will be added dynamically later.
             Original Tools and Move panels are preserved in repository history
             and can be restored when needed. -->
        
        <!-- Fade Overlay for Android Navigation Transitions -->
        <BoxView x:Name="FadeOverlay"
                 Grid.ColumnSpan="3"
                 Grid.RowSpan="2"
                 Color="Black"
                 Opacity="0"
                 IsVisible="False"
                 ZIndex="10000"
                 InputTransparent="True"
                 HorizontalOptions="Fill"
                 VerticalOptions="Fill"
                 Margin="-10"/>
        
    </Grid>
    
</ContentPage>
